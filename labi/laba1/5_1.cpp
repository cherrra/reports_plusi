#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double s=0, x = -15.246, y = 4.642e-2, z = 21;
    s = log(pow(y, abs(sqrt(-x)))) * (x - (y/2)) + pow(sin(atan(z)), 2);
cout<< "S = "<<s;
    return 0;
}
